<p style="color: green"><%= notice %></p>

<%= render @coin %>

<div>
  <%= link_to t('edit'), edit_coin_path(@coin) %> |
  <%= link_to t('back'), coins_path %> |
  <%= link_to t('destroy'), coin_path, data: {'turbo-method': :delete, 'turbo-confirm': t('confirmation_msg')} %>
</div>
